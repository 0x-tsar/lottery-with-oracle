import React, { useEffect, useState } from "react";
import styled from "styled-components";

export const Container = styled.div`
  background-color: goldenrod;
  min-height: calc(100vh - 80px);
`;

const Main = ({ info }) => {
  return <Container>we</Container>;
};

export default Main;
