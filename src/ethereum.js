const randmNumberContract = require("./contracts/RandomNumberConsumer.json");
import web3 from "web3";

export default loadEthereum = async () => {
  window.addEventListener("load", () => {
    new Promise((resolve, reject), () => {});
  });
};
